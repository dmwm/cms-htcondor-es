apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-cephfs-claim
  namespace: spider
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1G
  storageClassName: meyrin-cephfs